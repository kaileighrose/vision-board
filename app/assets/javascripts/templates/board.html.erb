<div id="template" class="container">
  <div ng-controller="BoardController as board">
    <div ng-repeat="resource in board.resources">
      <div class="slot">
        {{resource}}
      </div>
    </div>
  </div>
</div>